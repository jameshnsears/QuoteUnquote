flags:
  utilslib.fdroid:
    - paths:
        - utilslib/src
    carryforward: true

  utilslib.googleplay:
    - paths:
        - utilslib/src
    carryforward: true

  cloudlib.googleplay:
    - paths:
        - cloudlib/src
    carryforward: true

  app.manifestTest:
    - paths:
        - app/src
    carryforward: true

  app.froid:
    - paths:
        - app/src
    carryforward: true

  app.gooleplay:
    - paths:
        - app/src
    carryforward: true
